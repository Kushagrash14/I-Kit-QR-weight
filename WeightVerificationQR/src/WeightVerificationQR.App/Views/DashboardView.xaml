<UserControl x:Class="WeightVerificationQR.App.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:WeightVerificationQR.App.ViewModels"
             xmlns:views="clr-namespace:WeightVerificationQR.App.Views"
             Background="{StaticResource BackgroundBrush}">

    <Grid Margin="28,24,28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- ============ Top bar: title + status pills ============ -->
        <Grid Grid.Row="0" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <TextBlock Text="Live Dashboard" Style="{StaticResource HeaderText}"/>
                <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                    <TextBlock Text="&#xE787;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBlock Text="{Binding CurrentDateTime, StringFormat='dddd, dd MMMM yyyy   •   HH:mm:ss'}" Style="{StaticResource SubHeaderText}"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                <Border Style="{StaticResource StatusPillConnected}" Margin="5,0">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="9" Height="9" Fill="{Binding MachineStatus, Converter={StaticResource ConnectionStatusToBrushConverter}}" VerticalAlignment="Center"/>
                        <TextBlock Text="Machine" Margin="7,0,0,0" FontSize="11.5" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource StatusPillConnected}" Margin="5,0">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="9" Height="9" Fill="{Binding PrinterStatus, Converter={StaticResource ConnectionStatusToBrushConverter}}" VerticalAlignment="Center"/>
                        <TextBlock Text="Printer" Margin="7,0,0,0" FontSize="11.5" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource StatusPillConnected}" Margin="5,0">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="9" Height="9" Fill="{Binding DatabaseStatus, Converter={StaticResource ConnectionStatusToBrushConverter}}" VerticalAlignment="Center"/>
                        <TextBlock Text="Database" Margin="7,0,0,0" FontSize="11.5" FontWeight="SemiBold" Foreground="{StaticResource TextSecondaryBrush}"/>
                    </StackPanel>
                </Border>
                <Button Style="{StaticResource SecondaryButton}" Command="{Binding ConnectCommand}" Margin="10,0,0,0" Padding="14,8">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE703;" FontFamily="Segoe MDL2 Assets" FontSize="12" VerticalAlignment="Center" Margin="0,0,7,0"/>
                        <TextBlock Text="Connect Machine" FontSize="12"/>
                    </StackPanel>
                </Button>

                <!-- Hardware-free demo/testing aid: types a weight and feeds it through the exact
                     same PASS/FAIL/QR/print pipeline a real scale reading would use. -->
                <Border Background="{StaticResource WarningLightBrush}" BorderBrush="#F0DFB4" BorderThickness="1" CornerRadius="8" Padding="10,5" Margin="10,0,0,0">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xEA80;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{StaticResource WarningBrush}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                        <TextBlock Text="Simulate (kg):" VerticalAlignment="Center" FontSize="11.5" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,6,0"/>
                        <TextBox Text="{Binding SimulatedWeightInput, UpdateSourceTrigger=PropertyChanged, StringFormat=0.000}" Width="72" Padding="6,3" FontSize="12"/>
                        <Button Content="Weigh" Style="{StaticResource SecondaryButton}" Command="{Binding SimulateWeighingCommand}" Margin="6,0,0,0" Padding="12,4" FontSize="12"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>

        <!-- ============ Metric cards row ============ -->
        <Grid Grid.Row="1" Margin="0,0,0,18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Current product -->
            <Border Grid.Column="0" Style="{StaticResource MetricCard}" Margin="0,0,9,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="42" Height="42" CornerRadius="10" Background="{StaticResource PrimaryLightBrush}" VerticalAlignment="Top">
                        <TextBlock Text="&#xE7B8;" FontFamily="Segoe MDL2 Assets" FontSize="17" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="13,0,0,0">
                        <TextBlock Text="CURRENT PRODUCT" Style="{StaticResource CardTitleText}"/>
                        <TextBlock Text="{Binding SelectedProduct.ProductName, TargetNullValue='No product selected'}" FontSize="14.5" FontWeight="SemiBold" Margin="0,4,0,0" TextWrapping="Wrap" TextTrimming="CharacterEllipsis" MaxHeight="42"/>
                        <TextBlock Margin="0,4,0,0" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11.5">
                            <Run Text="Range:"/>
                            <Run Text="{Binding SelectedProduct.MinWeightKg, StringFormat=0.000}" FontWeight="SemiBold"/>
                            <Run Text="–"/>
                            <Run Text="{Binding SelectedProduct.MaxWeightKg, StringFormat=0.000}" FontWeight="SemiBold"/>
                            <Run Text="kg"/>
                        </TextBlock>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Pass today -->
            <Border Grid.Column="1" Style="{StaticResource MetricCard}" Margin="9,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="42" Height="42" CornerRadius="10" Background="{StaticResource SuccessLightBrush}" VerticalAlignment="Top">
                        <TextBlock Text="&#xE73E;" FontFamily="Segoe MDL2 Assets" FontSize="17" Foreground="{StaticResource SuccessBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="13,0,0,0">
                        <TextBlock Text="TOTAL PASS TODAY" Style="{StaticResource CardTitleText}"/>
                        <TextBlock Text="{Binding TotalPassToday}" Style="{StaticResource MetricValueText}" Foreground="{StaticResource SuccessBrush}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Fail today -->
            <Border Grid.Column="2" Style="{StaticResource MetricCard}" Margin="9,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="42" Height="42" CornerRadius="10" Background="{StaticResource DangerLightBrush}" VerticalAlignment="Top">
                        <TextBlock Text="&#xE711;" FontFamily="Segoe MDL2 Assets" FontSize="17" Foreground="{StaticResource DangerBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="13,0,0,0">
                        <TextBlock Text="TOTAL FAIL TODAY" Style="{StaticResource CardTitleText}"/>
                        <TextBlock Text="{Binding TotalFailToday}" Style="{StaticResource MetricValueText}" Foreground="{StaticResource DangerBrush}"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Last printed QR -->
            <Border Grid.Column="3" Style="{StaticResource MetricCard}" Margin="9,0,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Border Grid.Column="0" Width="42" Height="42" CornerRadius="10" Background="{StaticResource WarningLightBrush}" VerticalAlignment="Top">
                        <TextBlock Text="&#xE749;" FontFamily="Segoe MDL2 Assets" FontSize="17" Foreground="{StaticResource WarningBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="13,0,0,0">
                        <TextBlock Text="LAST PRINTED QR" Style="{StaticResource CardTitleText}"/>
                        <TextBlock Text="{Binding LastPrintedQr.QrId, TargetNullValue='—'}" FontSize="14.5" FontFamily="{StaticResource MonoFont}" FontWeight="SemiBold" Margin="0,5,0,0" TextTrimming="CharacterEllipsis"/>
                        <TextBlock Text="{Binding OperatorName, StringFormat='Operator: {0}'}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="11.5" Margin="0,4,0,0" TextTrimming="CharacterEllipsis"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- ============ Main content: product selector + live/pass/fail panel ============ -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="272"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Style="{StaticResource CardBorder}" Margin="0,0,18,0" Padding="14">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="4,2,0,12">
                        <TextBlock Text="&#xE71D;" FontFamily="Segoe MDL2 Assets" FontSize="13" Foreground="{StaticResource TextMutedBrush}" VerticalAlignment="Center" Margin="0,0,7,0"/>
                        <TextBlock Text="SELECT PRODUCT" Style="{StaticResource CardTitleText}" VerticalAlignment="Center"/>
                    </StackPanel>
                    <ListBox ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct, Mode=TwoWay}" IsSynchronizedWithCurrentItem="True"
                             BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,3"/>
                                <Setter Property="Cursor" Value="Hand"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="bd" CornerRadius="8" Background="Transparent" BorderThickness="1.4" BorderBrush="Transparent">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="bd" Property="Background" Value="{StaticResource SurfaceAltBrush}"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="bd" Property="Background" Value="{StaticResource PrimaryLightBrush}"/>
                                                    <Setter TargetName="bd" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Margin="12,10">
                                    <TextBlock Text="{Binding ProductName}" TextWrapping="Wrap" FontWeight="SemiBold" FontSize="12.5" Foreground="{StaticResource TextPrimaryBrush}"/>
                                    <TextBlock FontSize="10.5" Foreground="{StaticResource PrimaryBrush}" FontWeight="SemiBold" Margin="0,4,0,0">
                                        <Run Text="{Binding CommandCode}"/><Run Text="-"/><Run Text="{Binding LabelLineCode}"/>
                                        <Run Text="  |  "/><Run Text="{Binding ModelCode}"/>
                                    </TextBlock>
                                    <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                        <TextBlock Text="{Binding Quantity}" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}"/>
                                        <TextBlock FontSize="11" Foreground="{StaticResource TextMutedBrush}" Margin="8,0,0,0">
                                            <Run Text="{Binding MinWeightKg, StringFormat=0.000}"/><Run Text="–"/><Run Text="{Binding MaxWeightKg, StringFormat=0.000}"/><Run Text=" kg"/>
                                        </TextBlock>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <Border Grid.Column="1" Style="{StaticResource CardBorder}" Padding="0">
                <Grid>
                    <!-- LIVE WEIGHT SCREEN (separate file: LiveWeightView.xaml) -->
                    <views:LiveWeightView>
                        <views:LiveWeightView.Style>
                            <Style TargetType="views:LiveWeightView">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DisplayState}" Value="Live">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding DisplayState}" Value="Idle">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </views:LiveWeightView.Style>
                    </views:LiveWeightView>

                    <!-- PASS SCREEN (separate file: PassView.xaml) -->
                    <views:PassView>
                        <views:PassView.Style>
                            <Style TargetType="views:PassView">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DisplayState}" Value="Pass">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </views:PassView.Style>
                    </views:PassView>

                    <!-- FAIL SCREEN (separate file: FailView.xaml) -->
                    <views:FailView>
                        <views:FailView.Style>
                            <Style TargetType="views:FailView">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DisplayState}" Value="Fail">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </views:FailView.Style>
                    </views:FailView>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
